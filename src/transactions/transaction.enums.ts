export enum TransactionType {
  Income = 'Income',
  Expense = 'Expense',
}

export enum TransactionInputMethod {
  Manual = 'Manual',
  Voice = 'Voice',
  OCR = 'OCR',
}

export enum TransactionStatus {
  Draft = 'Draft',
  Confirmed = 'Confirmed',
}
